import React from 'react'

export const Divider = ({section}) => {
  return (
    <div className='divider'>
      <canvas></canvas>
      <h2>{ section }</h2>
      <canvas></canvas>
    </div>
  )
}
